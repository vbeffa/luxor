# Luxor Interview

## How to run

First start the database with:

`docker-compose up`

Then

`go run cmd/luxor/main.go`

## How to test

First start the database with:

`docker-compose up`

Then

`ginkgo -v`
